# PLAN — Issues 006 + 007 + 008 + 009: Limpeza de Inconsistências

**Escopo:** Remover módulo transporte, popular gráficos, limpar código morto, padronizar cultures
**Status:** Aguardando aprovação

---

## 1. Arquivos a Modificar

### [MODIFY] maquina-detalhes.tsx
`src/pages/maquina-detalhes.tsx`

**Mudanças (006):**
- Remover import de `DEMO_TRANSPORTS` (linha 13) e `Truck` (linha 28)
- Remover `transports` dos cálculos no `stats` useMemo (linhas 257, 283, 291, 323)
- Remover `totalTons` do retorno de stats e do KPI card (linha 444)
- Remover `TabsTrigger value="transports"` (linha 501)
- Remover `TabsContent value="transports"` inteiro (linhas 637-675)
- Alterar `getRevenueRedirect`: `/transporte` → `/colheita` (linha 334)

**Mudanças (009):**
- Linha 580: `h.culture` → `h.cultures?.join(", ")`

---

### [MODIFY] demo-data.ts
`src/lib/demo-data.ts`

**Mudanças (007):**
- Adicionar mais registros a `DEMO_HARVESTS` para variar `machineId` (garantir que máquinas 1, 2, 4 tenham 3+ registros)
- Já tem 8 registros; verificar distribuição:
  - machineId=1: 2 registros ✅
  - machineId=2: 2 registros ✅
  - machineId=3: 1 registro
  - machineId=4: 2 registros ✅
- Precisa adicionar ~2-3 registros para máquinas 3 e com mais datas variadas

---

### [MODIFY] AppLayout.tsx
`src/components/layout/AppLayout.tsx`

**Mudanças (008):**
- Remover `"/transporte": "Transporte"` do mapa de títulos (linha 12)

---

### [DELETE] MachineDetailSheet.tsx
`src/components/MachineDetailSheet.tsx`

**Mudanças (008):**
- Deletar arquivo inteiro (código morto, 0 imports)

---

### [MODIFY] talhoes/detalhes.tsx
`src/features/talhoes/detalhes.tsx`

**Mudanças (009):**
- Linhas 438, 463: `h.culture` → `h.cultures?.join(", ")`

---

### [MODIFY] cultura-detalhes.tsx
`src/pages/cultura-detalhes.tsx`

**Mudanças (009):**
- Linha 117: `h.culture.toLowerCase()` → filtrar via `h.cultures?.some(c => c.toLowerCase() === cultura.name.toLowerCase())`

---

### [MODIFY] dashboard.tsx
`src/pages/dashboard.tsx`

**Mudanças (008):**
- Avaliar se `DEMO_TRANSPORTS` é usado em algum card visível. Se não → remover import.

---

## 2. Dependências

- **Nenhuma dependência nova.** São todas remoções/padronizações.

---

## 3. Ordem de Execução

1. **006**: Remover aba transporte do `maquina-detalhes.tsx`
2. **008**: Limpar `AppLayout.tsx`, deletar `MachineDetailSheet.tsx`, limpar dashboard
3. **007**: Adicionar registros mock ao `DEMO_HARVESTS`
4. **009**: Padronizar `h.cultures` em todas as telas

---

## 4. Checklist Final

- [ ] maquina-detalhes.tsx: sem aba Transporte, sem DEMO_TRANSPORTS, culture padronizado
- [ ] AppLayout.tsx: sem entrada "/transporte"
- [ ] MachineDetailSheet.tsx: deletado
- [ ] demo-data.ts: mais harvests com machineIds variados
- [ ] talhoes/detalhes.tsx: h.cultures
- [ ] cultura-detalhes.tsx: filtrar pelo array
- [ ] dashboard.tsx: limpo de refs desnecessárias
- [ ] Verificação visual no browser
